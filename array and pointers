#include<stdio.h>
int main(){
void inputArray(int*arr,int n);
void displayArray(int*arr,int n);
int sumArray(int*arr,int n);
int countPositive(int*arr,int n);
int countNegative(int*arr,int n);
int main(){
int arr[100];
int n,sum,pos_C,neg_C;
printf("Enter the number if elements in array");
scanf("%d",&n);
inputArray(arr,n);
printf("\nArray elements are:");
displayArray(arr,n);
sum=sumArray(arr,n);
printf("\nsum of all elements=%d",sum);
 posCount=countpositive(arr,n);
negCount=countNegative(arr,n);
printf("\nNumber of positive elements=%d",poscount);
printf("\nNumber of negative elements=%d",negcount);
return 0;

}
void input Array (int *arr,intn){
.printf("enter %d elements:\n",n);
for(int i=0;i<n;i++){
    scanf("%d",*(arr+i));
}
}
void displayArray(const int*arr,int)

